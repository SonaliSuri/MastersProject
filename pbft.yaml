nodes:
    - host: localhost
      port: 30100
    - host: localhost
      port: 30101
    - host: localhost
      port: 30102
    - host: localhost
      port: 30103

clients:
    - host: localhost
      port: 20101
    - host: localhost
      port: 20102

loss%: 0

ckpt_interval: 10

retry_times_before_view_change: 2

sync_interval: 5

misc:
    network_timeout: 5
